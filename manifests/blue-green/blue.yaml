apiVersion: apps/v1
kind: Deployment
metadata:
  name: echo-blue
spec:
  replicas: 2
  selector:
    matchLabels:
      app: echo
      version: blue
  template:
    metadata:
      labels:
        app: echo
        version: blue
    spec:
      containers:
      - name: echo
        image: ealen/echo-server:0.1
        readinessProbe:
          httpGet: { path: "/", port: 80 }
        livenessProbe:
          httpGet: { path: "/", port: 80 }
